# Elasticsearch Configuration
ELASTICSEARCH_HOST=localhost:9200
ELASTICSEARCH_USERNAME=
ELASTICSEARCH_PASSWORD=
ELASTICSEARCH_API_KEY=
ELASTICSEARCH_INDEX=banking_transactions

# Authentication Options:
# Option 1: Username/Password (Basic Auth)
# ELASTICSEARCH_USERNAME=elastic
# ELASTICSEARCH_PASSWORD=your-password

# Option 2: API Key (Recommended for production)
# ELASTICSEARCH_API_KEY=your-api-key

# Optional: If using Elasticsearch Cloud
# ELASTICSEARCH_HOST=your-cluster.es.region.aws.cloud.es.io:443

# LM Studio Configuration (for AI-enhanced transaction generation)
LM_STUDIO_HOST=http://localhost:1234
LM_STUDIO_MODEL=

# LM Studio Options:
# Default host is http://localhost:1234 (LM Studio default)
# Model name is optional - will use the currently loaded model if not specified
# Example: LM_STUDIO_MODEL=llama-2-7b-chat
